import React from 'react';
import PropTypes from 'prop-types';
import makeClass from 'classnames';
import {
  Card,
  Header,
  Image,
  Item,
  Loader,
  Message,
  Segment,
} from 'semantic-ui-react';

/**
 * @func PieceDetail
 * @desc The piece detail screen provides more information and images
*        related to a given piece.
 * @param {object} props 
 * @returns {Component}
 */
export function PieceDetail(props) {
  return (
    <div>
      PieceDetail
    </div>
  );
}

PieceDetail.propTypes = {};

export default PieceDetail;